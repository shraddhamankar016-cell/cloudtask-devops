apiVersion: v1
kind: Service
metadata:
  name: cloudtask-backend
  namespace: cloudtask
  labels:
    app: cloudtask-backend
spec:
  type: ClusterIP
  selector:
    app: cloudtask-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
