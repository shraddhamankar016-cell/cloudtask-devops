apiVersion: v1
kind: Service
metadata:
  name: cloudtask-frontend
  namespace: cloudtask
  labels:
    app: cloudtask-frontend
spec:
  type: ClusterIP
  selector:
    app: cloudtask-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
