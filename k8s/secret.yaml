# Example only — do NOT commit real secrets to git.
# In production, use AWS Secrets Manager + the External Secrets
# Operator, or `kubectl create secret` at deploy time via CI/CD.
apiVersion: v1
kind: Secret
metadata:
  name: cloudtask-secrets
  namespace: cloudtask
type: Opaque
stringData:
  DB_PASSWORD: "changeme-in-production"
